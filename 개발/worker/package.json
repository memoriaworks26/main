{
  "name": "memoriaworks-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "메모리아웍스 렌더 워커 — 큐(submissions.queued) 소비 → AI·FFmpeg 렌더 → done",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/index.js",
    "once": "node src/index.js --once",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
